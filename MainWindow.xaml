<Window x:Class="ITEQ2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ITEQ2"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="800" Background="#333333">
    <Window.Resources>
    </Window.Resources>
    <Grid>

        
        
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>


        <StackPanel>
            <Button Name="btnNormal" Content="Normal" Click="btnNormal_Click"
                    Width="100" Height="40" Margin="5"/>
            <Button Name="btnModal" Content="Modal" Click="btnModal_Click"
                    Width="100" Height="40" Margin="5"/>
            <TextBox x:Name="txtInput" Width="200" Height="40" FontSize="20"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <StackPanel VerticalAlignment="Center">
                <!--<StackPanel.Resources>
                    <Style TargetType="TextBox">
                        <Setter Property="Width" Value="200"/>
                        <Setter Property="Height" Value="40"/>
                        <Setter Property="FontWeight" Value="Light"/>
                        <Setter Property="FontSize" Value="20"/>
                        <Setter Property="Margin" Value="5"/>
                    </Style>
                </StackPanel.Resources>-->
                <TextBox/>
                <TextBox/>
                <TextBox/>
                <Button Content="Normal" Margin="0,5,0,0"/>
                <Button Content="Normal" Margin="0,5,0,0"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="2">
            <!--<Button VerticalAlignment="Top" Style="{StaticResource ConfirmationButton}"/>-->
        </Grid>

        <ScrollViewer Grid.Row="2">
            <Button Content="im a button" Width="100" Height="40"/>
        </ScrollViewer>



    </Grid>
</Window>
